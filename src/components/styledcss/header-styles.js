import styled from "@emotion/styled";

export const StyledHeader = styled("div")({
	height: "50px",
	background: "#f1f1f1",
	display: 'flex',
	alignItems: 'center',
	padding: '2px 20px',

	img: {
		width: 'auto',
		height: '100%',
		marginRight: '20px'
	}
});
